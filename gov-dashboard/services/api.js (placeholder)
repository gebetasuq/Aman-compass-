import axios from 'axios';

const api = axios.create({
  baseURL: process.env.NEXT_PUBLIC_API_URL,
});

export const fetchDashboardData = (emirate = 'all') => {
  return api.get(`/dashboard/summary?emirate=${emirate}`);
};
