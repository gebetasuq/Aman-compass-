import 'package:flutter/material.dart';

class DataExplorerScreen extends StatelessWidget {
  const DataExplorerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Data Explorer – Coming Soon'),
    );
  }
}
